const title = document.getElemenstByTagName('h1');
/* devuelve un arreglo de los elementos que tengan esa etiquta
*/
/*title[0].innerHTML= 'TITULO CAMBIADO';

const texto = document.getElementById('text');

texto.innerHTML = 'esate texto  esta escrito desde javascrip';
texto.style.background = 'blue';

console.log(texto);

*/

/*obtiene un elemento por id */
document.getElementById
/**obtiene todos los elementos de una clase y devuelve un arreglo */
document.getElementsByClassName
/**se obtiene todos los elements por nombre y devuelve un arreglo */
document.getElementsByName


const texo = document.querySelector('#text');
Texto.textContent = 'iestoy escribiendo desde javascript';

/* crear elementos html */
const div = document.create